<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- =========================
         PROPERTY DEFINITIONS
         ========================= -->
    <property name="LOG_PATH" value="logs"/>
    <property name="FILE_NAME" value="financeforge"/>
    <property name="MAX_FILE_SIZE" value="10MB"/>
    <property name="MAX_HISTORY" value="30"/>

    <!-- =========================
         CONSOLE APPENDER (DEV)
         ========================= -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- =========================
         FILE APPENDER (PROD)
         ========================= -->
    <appender name="File" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${FILE_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${FILE_NAME}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- =========================
         LOGGER LEVELS
         ========================= -->
    <!-- Your repositories at DEBUG -->
    <logger name="com.financeforge.api.repository" level="DEBUG"/>

    <!-- Services at INFO -->
    <logger name="com.financeforge.api.service" level="INFO"/>

    <!-- Spring framework internal logs at WARN -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>

    <!-- =========================
         ROOT LOGGER
         ========================= -->
    <root level="INFO">
        <appender-ref ref="Console"/>
        <appender-ref ref="File"/>
    </root>

</configuration>
